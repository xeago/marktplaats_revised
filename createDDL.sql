CREATE TABLE users (id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, email_address VARCHAR(255), password VARCHAR(255), username VARCHAR(255), PRIMARY KEY (id))
CREATE TABLE advertisements (id INTEGER GENERATED BY DEFAULT AS IDENTITY NOT NULL, date VARCHAR(255), description VARCHAR(255), price VARCHAR(255), status INTEGER, title VARCHAR(255), ADVERTISER_id INTEGER, PRIMARY KEY (id))
ALTER TABLE advertisements ADD CONSTRAINT dvrtsmentsDVRTSRid FOREIGN KEY (ADVERTISER_id) REFERENCES users (id)
